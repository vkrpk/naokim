{% set navLinkClasses = "nav-link link-white d-inline-block" %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{asset('navbar.css')}}">
{% endblock %}

<header class="navbar bg-secondary" id="header" style="min-height:56px">
	<nav class="d-flex justify-content-between align-items-center container" id="navbar-lg">
		<div class="w-30 d-flex">
			<a class="navbar-brand ps-0 {{ navLinkClasses }}" href="{{path('home')}}">LOGO</a>
		</div>
		<div class="w-30 d-flex">
			<form class="d-flex input-group">
				<input type="search" class="form-control border-0 rounded-pill" placeholder="Rechercher un produit" aria-label="search" style="width: 250px;">
				<button type="submit" class="input-group-text text-secondary bg-transparent border-0 rounded-pill" style="position: absolute; right: 0;">
					<i class="bi bi-search"></i>
				</button>
			</form>
		</div>
		<div class="w-30 d-flex">
			<ul class="d-flex">
				{% include "shared/navbar/list-link.html.twig" %}
			</ul>
		</div>
	</nav>
	<div id="side-bar" class="bg-secondary bg-gradient">
		<div class="toggle-button" id="toggle-button">
			<span></span>
			<span></span>
			<span></span>
		</div>

		<ul class="d-flex flex-column fs-2 bg-secondary bg-gradient vh-100 align-items-end">
			<form class="nav-link position-relative mt-4" style="width:250px">
				<input type="search" class="form-control border-0 rounded-pill" placeholder="Rechercher un produit" aria-label="search">
				<button type="submit" class="input-group-text text-secondary bg-transparent border-0 rounded-pill position-absolute end-0 top-50 translate-middle">
					<i class="bi bi-search"></i>
				</button>
			</form>
			<li class="">
				<a href="{{path('product_all')}}" class="{{ navLinkClasses }}">Produits</a>
			</li>
			<li class="">
				<a href="{{path('service_all')}}" class="{{ navLinkClasses }}">Services</a>
			</li>
			<li class="nav-item">
				<a href="{{path('cart_show')}}" class="{{ navLinkClasses }} pb-0">
					<i class="bi bi-cart me-1"></i>
					{{cartService.detailedCartItems|length > 0 ? cartService.detailedCartItems|length }}
				</a>
			</li>
			{% if app.user %}
				<li class="nav-item">
					<a href="{{ path('purchase_index') }}" class="{{ navLinkClasses }}">Mes commandes</a>
				</li>
				<li class="nav-item">
					<a href="{{path('app_logout')}}" class="{{ navLinkClasses }}">Déconnexion</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a href="{{path('app_login')}}" class="{{ navLinkClasses }}">Connexion</a>
				</li>
				<li class="nav-item">
					<a href="{{path('app_login')}}" class="{{ navLinkClasses }}">Inscription</a>
				</li>
			{% endif %}
		</ul>
	</div>
</header>
