{% extends 'base.html.twig' %}

{% block title %}Éditer
	{{product_category.name}}
{% endblock %}

{% block body %}
	<h3>Éditer
		{{product_category.name}}</h3>
	{{ include('product_category/_form.html.twig', {edit:true}) }}
	<p class="my-2">
		{{ include('_btn/_btn-back.html.twig', {path:'product_category_index'}) }}
	</p>
{% endblock %}
