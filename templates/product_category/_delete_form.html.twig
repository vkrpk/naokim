<form class="d-inline" method="post" action="{{ path('product_category_delete', {'id': product_category.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cet enregistrement?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_category.id) }}">
	{{ include('_btn/_btn-delete.html.twig') }}
</form>
